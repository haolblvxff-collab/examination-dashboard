This directory contains browser-side assets used while the packaged dashboard is offline.

`echarts.min.js` is supplied from the Apache ECharts distribution during packaging.
When it is absent from a source checkout, the HTML page falls back to the official CDN distribution.
